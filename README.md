# WorkflowEngine

A minimal .NET 8 Web API for configurable workflows as state machines.

## Prerequisites
- .NET 8 SDK installed

## Quick Start
```bash
cd WorkflowEngine
dotnet restore
dotnet run
